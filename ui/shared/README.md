# Shared UI Libraries

Cross-cutting utilities, components, and types for all UIs.

Aqevia Web UI is a single SPA served by the Engine at `/`, and these shared libraries support `/admin/*`, `/builder/*`, and `/client/*` while keeping the experience unified.
